#include "monty.h"

/**
 * nop_op - Function that deos nothig
 * @stack: double pointer to the head of the stack
 * @line_number: line number command found
 *
 * Return: nothing.
 */
void nop_op(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
